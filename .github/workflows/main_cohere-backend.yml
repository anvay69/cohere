name: Build and deploy FastAPI Backend

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install 3.13

      - name: Sync dependencies
        run: uv sync --locked

      # FIX: Zip the CONTENTS of the backend folder into a clean archive at the repository root
      - name: Zip artifact for deployment
        run: zip -r ../backend-deploy.zip ./* -x ".venv/*"

      # Upload the flat zip package instead of the folder directory
      - name: Upload backend
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: backend-deploy.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # FIX: Point the package directly to your zipped artifact name
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: Cohere-Backend
          slot-name: Production
          package: backend-deploy.zip